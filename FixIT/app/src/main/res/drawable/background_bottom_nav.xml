<!-- background_bottom_nav.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
     />
    <gradient
        android:startColor="@color/light_grey"
        android:endColor="@color/white"
        android:angle="-90"/>

    <stroke
        android:color="#000"
        android:width="0.5dp"/>
    <corners android:radius="20dp" /> <!-- Adjust the radius value as per your preference -->
</shape>
